
#1.如果用户的一卡通丢失后，必须首先到一卡通管理中心办理挂失
   手续，挂失后，该账户就处于冻结状态，该用户的一卡通就不会被
   他人盗用
#2.下面给出“一卡通挂失”的活动流。主流：用户在中心计算机上
   输入自己的卡号和密码，然后中心计算机将卡号和密码传到中心数
   据库上进
#3.行验证，验证通过后，在中心计算机上显示用户信息。一卡通管
   理员在用户信息上选择冻结账户，然后中心计算机将修改的信息写
   入中心数
#4据库，中心数据库对用户信息进行保存，保存完毕后，中心数据
   库向中心计算机返回冻结帐户成功的信息，该信息在中心计算机上
   显示出来.
#5一卡通管理员看到此信息后，就告诉用户:挂失成功。异常情形：
   如果用户输入的卡号或密码不正确，那么就不能通过中心数据库的
   验证，这
#6样用户都不能挂失。当用户的一卡通找到后，由于已经挂失了，
   以卡通处于冻结状态，要想重新使用该一卡通，应当再到一卡通管
   理中心办
#7理解挂，解挂后，一卡通就处于活动状态，用户就可以再次使用
   它了。
#8.下面给出“一卡通解挂”的活动流。主流：用户首先向一卡通管
   理员提出解挂请求，然后将一卡通交给一卡通管理员，然后一卡通
   管理员在
#9刷卡机上刷卡，刷卡通过后，刷卡机从数据库中提取卡信息，然
   后卡信息显示在中心计算机上。一卡通管理员在用户信息上选择激
   活账户，
#10然后中心计算机将修改的信息写入中心数据库，中心数据库对用
   户信息进行保存，保存完毕后,中心数据库对一卡通信息进行更新，
   更新完
#11.下面给出“一卡通解挂”的活动流。主流：用户首先向一卡通
   管理员提出解挂请求，然后将一卡通交给一卡通管理员，然后一卡
   通管理员
#12卡通.后，中心数据库向中心计算机返回解挂帐户成功的信息，
   该信息在中心计算机上显示出来，一卡通管理员看到此信息后，将
   一卡通交
#13给用户，解挂成功。异常情形：如果一卡通损坏，那么就不能通
   过刷卡机的验证，这样用户都不能解挂。当用户毕业时，要将一卡
   通退还给
#14以卡通管理中心。如果账户上还有余额，则将余额退还给用户。
   下面给出“退卡”的活动流。主流：用户首先向一卡通管理员提出
   退卡请求
#15，然后将一.卡通交给一卡通管理员，然后一卡通管理员在刷卡
   机上刷卡，刷卡通过后，刷卡机从数据库中提取卡信息，然后卡信
   息显示在
#16中心计算机上。然后用户在中心计算机上输入一卡通密码，密码
   在中心数据库上验证通过后，再计算用户的余额并且返回给中心计
   算机，一
#17卡通管理员在用户信息上选择删除用户信息，然后中心计算机将
   改动写入中心数据库，中心数据库进行保存，保存完毕后,中心数据
   库将一
#18卡通上的信息清空，.清空完一卡通上信息后，中心数据库向中
   心计算机返回退卡成功的信息，一卡通管理员看到此信息后，将余
   额退还给
#19异常情形：如果一卡通损坏，那么就不能通过刷卡机的验证，这
   样用户都不能退卡；如果用户退卡时输入的密码有误，用户也不能
   退卡。
#20如果用户的一卡通丢失后，必须首先到一卡通管理中心办理挂失
   手续，挂失后，该账户就处于冻结状态，该用户的一卡通就不会被
   他人盗用
#21下面给出“一卡通挂失”的活动流。主流：用户在中心计算机上
   输入自己的卡号和密码，然后中心计算机将卡号和密码传到中心数
   据库上进
#22行验证，验证通过后，在中心计算机上显示用户信息。一卡通管
   理员在用户信息上选择冻结账户，然后中心计算机将修改的信息写
   入中心据
#23库，中心数据库对用户信息进行保存，保存完毕后，中心数据库
   向中心计算机返回冻结帐户成功的信息，该信息在中心计算机上显
   示出来，
#24一卡通管理员看到此信息后，就告诉用户:挂失成功。异常情形
   ：如果用户输入的卡号或密码不正确，那么就不能通过中心数据库
   的验证，
#25这样用户都不能挂失。当用户的一卡通找到后，由于已经挂失了
   ，以卡通处于冻结状态，要想重新使用该一卡通，应当再到一卡通
   管理中心
#26办理解挂，解挂后，一卡通就处于活动状态，用户就可以再次使
   用它了。
#27下面给出“一卡通解挂”的活动流。主流：用户首先向一卡通管
   理员提出解挂请求，然后将一卡通交给一卡通管理员，然后一卡通
   管理员在
#28刷卡机上刷卡，刷卡通过后，刷卡机从数据库中提取卡信息，然
   后卡信息显示在中心计算机上。一卡通管理员在用户信息上选择激
   活账户，
#29然后中心计算机将修改的信息写入中心数据库，中心数据库对用
   户信息进行保存，保存完毕后,中心数据库对一卡通信息进行更新，
   更新完
#30后，中心数据库向中心计算机返回解挂帐户成功的信息，该信息
   在中心计算机上显示出来，一卡通管理员看到此信息后，将一卡通
   交给用户
#31解挂成功。异常情形：如果一卡通损坏，那么就不能通过刷卡机
   的验证，这样用户都不能解挂。当用户毕业时，要将一卡通退还给
   以卡通管
#32理中心。如果账户上还有余额,则将余额退还给用户。下面给出
   “退卡”的活动流。主流：用户首先向一卡通管理员提出退卡请求
   ,然后将
#33.卡通交给一卡通管理员，然后一卡通管理员在刷卡机上刷卡，
   刷卡通过后，刷卡机从数据库中提取卡信息，然后卡信息显示在中
   心计算机
#34上。然后用户在中心计算机上输入一卡通密码，密码在中心数据
   库上验证通过后，再计算用户的余额并且返回给中心计算机，一卡
   通管理员
#35在用户信息上选择删除用户信息，然后中心计算机将改动写入中
   心数据库，中心数据库进行保存，保存完毕后,中心数据库将一卡通
   上的信
#36息清空清空完一卡通上信息后，中心数据库向中心计算机返回退
   卡成功的信息，一卡通管理员看到此信息后，将余额退还给用户，
   用户退卡
#37异常情形：如果一卡通损坏，那么就不能通过刷卡机的验证，这
   样用户都不能退卡；如果用户退卡时输入的密码有误，用户也不能
   退卡。
#